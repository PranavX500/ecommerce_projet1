✅ Create a backend endpoint to place an order.  
✅ Retrieve the user's `_id` using their email.  
✅ Store order details in the MongoDB `orders` collection.  

## Steps for Milestone 25 📝  
1. Create an API endpoint that accepts product details, user information, and address details.  
2. Retrieve the `_id` of the user from the database using their email.  
3. For each product in the order, create a separate order entry with the same address.  
4. Save the order details in the `orders` collection using the previously created order schema.  
